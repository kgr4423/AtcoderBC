#include<bits/stdc++.h>
using namespace std;

int main(){
    
    int n, m;
    cin >> n >> m;

    vector<int> k(m+1);
    vector<vector<int>> x(100, vector<int>(100)); 
    for(int i=1; i<=m; ++i){
        cin >> k[i];
        
    }

    
}

