#include <bits/stdc++.h>
using namespace std;

int main(){
    //入力
    int h1, w1;
    cin >> h1, w1;

    vector<vector<long long> > A(h1, vector<long long>(w1));
    for(int i=0; i<h1; ++i){
        for(int j=0; j<w1; ++j){
            cin >> A.at(i).at(j);
        }
    }

    int h2, w2;
    cin >> h2, w2;

    vector<vector<long long> > B(h2, vector<long long>(w2));
    for(int i=0; i<h2; ++i){
        for(int j=0; j<w2; ++j){
            cin >> B.at(i).at(j);
        }
    }

    //主処理
    


}


